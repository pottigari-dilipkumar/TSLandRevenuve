services:
  backend:
    build:
      context: ./app-backend
    container_name: land-revenue-backend
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./frontend
    container_name: land-revenue-frontend
    depends_on:
      - backend
    ports:
      - "8081:80"
